# ──────────────────────────────────────────────────────────
#  JWT Authentication Configuration — DISABLED
#  Auth is bypassed for now. Will be replaced by Keycloak OIDC.
#
#  Future flow (Keycloak):
#    Service → Keycloak /token → gets token
#    → calls API with token → APISIX validates against Keycloak
#
#  Applied via: make auth  (or bash scripts/apply-auth.sh)
# ──────────────────────────────────────────────────────────

# Consumers and JWT sign route are disabled.
# Uncomment / replace with openid-connect plugin config when Keycloak is ready.

# --- Previously active consumers (kept for reference) ---
# consumers:
#   - _meta:
#       resource: consumers
#       id: vzone_platform
#     username: vzone_platform
#     desc: "VZone platform service account"
#     plugins:
#       jwt-auth:
#         key: vzone_platform_key
#         secret: "${JWT_SECRET:-vzone-dev-secret-change-in-prod}"
#         algorithm: HS256
#         exp: 86400
#         base64_secret: false
#
#   - _meta:
#       resource: consumers
#       id: vzone_admin
#     username: vzone_admin
#     desc: "VZone admin user"
#     plugins:
#       jwt-auth:
#         key: vzone_admin_key
#         secret: "${JWT_SECRET_ADMIN:-vzone_admin-secret-change-in-prod}"
#         algorithm: HS256
#         exp: 3600
#         base64_secret: false
#
# jwt_sign_route:
#   _meta:
#     resource: routes
#     id: "900"
#   name: jwt-sign-endpoint
#   desc: "Public API for JWT token signing (dev/test only)"
#   uri: /apisix/plugin/jwt/sign
#   plugins:
#     public-api: {}
#   status: 1
